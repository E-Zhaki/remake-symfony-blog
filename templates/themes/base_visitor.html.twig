<!DOCTYPE html>
<html lang="fr">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>{% block title %}{% endblock %} - Blog de Jean Dupont</title>


            {# SEO #}

            <meta name='robots' content="index, follow">
            <meta name='description' content="{% block description %}{% endblock %}">
            <meta name='keywords' content="{% block keywords %}{% endblock %}">
            <meta name='author' content="Dwwm22">
            <meta name='publisher' content="Dwwm22">


            {# Webpack Encore #}

            {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            {% endblock %}

            {% block javascripts %}
                {{ encore_entry_script_tags('app') }}
            {% endblock %}

            {# Favicom #}

            <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
            <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
            <link rel="shortcut icon" href="/favicon.ico" />
            <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
            <link rel="manifest" href="/site.webmanifest" />

            {# Google Font #}
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Courgette&family=Unna:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
            


        </head>
        <body class = 'bg-primary'>

        {# Naxbar #}
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{path('app_visitor_welcome')}}">JD</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{path('app_visitor_welcome')}}">Acceuil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('app_visitor_welcome')}}#aboute-me">Qui suis-je?</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('app_visitor_blog_index')}}">Blog</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href='{{path('app_visitor_contact_index')}}'>Contact</a>
                        </li>
                    </ul>
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="#">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href='#'>Inscription</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        {# Main #}

        {# Footer #}

        <h1>Hello World!</h1>
            
        </body>
</html>